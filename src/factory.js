/**
 * Initializes new numas array filled with given value
 * 
 * @param {number} value
 * @param {object} shape
 * @param {string} datatype 
 * @returns {object}
 */
const fill = (value, shape, datatype) => {

}

/**
 * Initializes new numas array filled zeroes
 * 
 * @param {object} shape
 * @param {string} datatype 
 * @returns {object}
 */
const zeroes = (value, shape, datatype) => {

}

/**
 * Initializes new numas array filled zeros
 * 
 * @param {object} shape
 * @param {string} datatype 
 * @returns {object}
 */
const zeros = (shape, datatype) => {

}

/**
 * Initializes new numas array filled ones
 * 
 * @param {object} shape
 * @param {string} datatype 
 * @returns {object}
 */
const ones = (shape, datatype) => {

}

/**
 * Initializes new numas array
 * 
 * @param {number} start
 * @param {number} stop
 * @param {number} step 
 * @returns {object}
 */
const linspace = (start, stop, step) => {

}

module.exports = {

}
